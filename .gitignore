.env
__pycache__
CLAUDE.md
.pytest_cache
.claude/settings.local.json
.vscode
rules
db_cache/
testing/fixtures/

# Model weight files (large binaries - never commit)
*.gguf
*.ggml
agent/models/
agent/agent/

# Runtime test output
testing/output/

# Python version file (local)
.python-version

# Notes and scratch files
notes.txt
NUL

# SearXNG runtime data (settings.yml IS tracked)
searxng/data/
searxng/cache/

# Slack channel plugin: bun-installed deps
tools/slack_channel/node_modules/

# Scratch spreadsheets from ad-hoc excel-tool runs
*.xlsx

# Probe file written and removed by test_env_is_order_independent.py; ignored
# so an interrupted run cannot leave it staged.
testing/test_zz_env_probe_generated.py
testing/test_zz_cache_a_generated.py
testing/test_zz_cache_b_generated.py
testing/test_zz_rag_gate_probe.py

# Left behind by `pip install -e .`, which is how `nemo` gets onto PATH.
*.egg-info/
