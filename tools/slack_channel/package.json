{
  "name": "nemo-slack-channel",
  "version": "0.1.0",
  "description": "Claude Code channel plugin that bridges Slack into the local CC session.",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "bun install --no-summary --silent && bun server.ts",
    "test": "bun test"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "@slack/bolt": "^4.0.0"
  },
  "devDependencies": {
    "@types/bun": "^1.1.0"
  }
}
